import React from 'react';
import Genres from '../../containers/Genres';

import './app.scss';

const App = () => (
  <div className="App">
    <header className="App-header">
      header
    </header>
    <Genres />
  </div>
);

export default App;
